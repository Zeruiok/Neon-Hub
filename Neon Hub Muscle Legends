local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

Library.ForceCheckbox = false
Library.ShowToggleFrameInKeybinds = true

local Window = Library:CreateWindow({
    Title = "Muscle Legends | Neon Hub",
    Footer = "v1.0",
    NotifySide = "Right",
    ShowCustomCursor = true,
})

local Tabs = {
    Main = Window:AddTab("Main", "settings"),
    Stats = Window:AddTab("Stats", "settings"),
    Client = Window:AddTab("Client", "settings"),
    Misc = Window:AddTab("Misc", "settings"),
    Server = Window:AddTab("Server", "settings"),
    Killer = Window:AddTab("Killer", "settings"),
    Stats2 = Window:AddTab("Stats 2", "settings"),
    Teleport = Window:AddTab("Teleport", "settings"),
    Credits = Window:AddTab("Credits", "settings"),
    ["UI Settings"] = Window:AddTab("UI Settings", "sliders-horizontal"),
}

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")

-- ============ MAIN TAB ============
local BrawlGroup = Tabs.Main:AddLeftGroupbox("Brawl")

local godModeToggle = false
BrawlGroup:AddToggle("GodMode", {
    Text = "God Mode (Brawl)",
    Default = false,
    Callback = function(Value)
        godModeToggle = Value
        if Value then
            task.spawn(function()
                while godModeToggle do
                    game:GetService("ReplicatedStorage").rEvents.brawlEvent:FireServer("joinBrawl")
                    task.wait(0)
                end
            end)
        end
    end
})

local autoJoinToggle = false
BrawlGroup:AddToggle("AutoJoin", {
    Text = "Auto Join Brawl",
    Default = false,
    Callback = function(Value)
        autoJoinToggle = Value
        if Value then
            task.spawn(function()
                while autoJoinToggle do
                    game:GetService("ReplicatedStorage").rEvents.brawlEvent:FireServer("joinBrawl")
                    task.wait(2)
                end
            end)
        end
    end
})

local FarmGymGroup = Tabs.Main:AddLeftGroupbox("Auto Farm Gym")

local MuscleKingBenchToggle = false
FarmGymGroup:AddToggle("MuscleKingBench", {
    Text = "Auto Muscle King Bench",
    Default = false,
    Callback = function(Value)
        MuscleKingBenchToggle = Value
        if Value then
            task.spawn(function()
                while MuscleKingBenchToggle do
                    game.Players.LocalPlayer.Character:SetPrimaryPartCFrame(CFrame.new(-8590.06152, 46.0167427, -6043.34717))
                    game:GetService("ReplicatedStorage").rEvents.machineInteractRemote:InvokeServer("useMachine", workspace.machinesFolder["Muscle King Bench"].interactSeat)
                    task.wait(0.1)
                end
            end)
        end
    end
})

local MuscleKingSquatToggle = false
FarmGymGroup:AddToggle("MuscleKingSquat", {
    Text = "Auto Muscle King Squat",
    Default = false,
    Callback = function(Value)
        MuscleKingSquatToggle = Value
        if Value then
            task.spawn(function()
                while MuscleKingSquatToggle do
                    game.Players.LocalPlayer.Character:SetPrimaryPartCFrame(CFrame.new(-8940.12402, 13.1642084, -5699.13477))
                    game:GetService("ReplicatedStorage").rEvents.machineInteractRemote:InvokeServer("useMachine", workspace.machinesFolder["Muscle King Squat"].interactSeat)
                    task.wait(0.1)
                end
            end)
        end
    end
})

local FarmJungleGroup = Tabs.Main:AddRightGroupbox("Auto Farm Jungle")

local jungleBenchToggle = false
FarmJungleGroup:AddToggle("JungleBench", {
    Text = "Auto Jungle Bench",
    Default = false,
    Callback = function(Value)
        jungleBenchToggle = Value
        if Value then
            task.spawn(function()
                while jungleBenchToggle do
                    game.Players.LocalPlayer.Character:SetPrimaryPartCFrame(CFrame.new(-8629.88086, 64.8842468, 1855.03467))
                    game:GetService("ReplicatedStorage").rEvents.machineInteractRemote:InvokeServer("useMachine", workspace.machinesFolder["Jungle Bench"].interactSeat)
                    task.wait(0.1)
                end
            end)
        end
    end
})

local jungleBarLiftToggle = false
FarmJungleGroup:AddToggle("JungleBarLift", {
    Text = "Auto Jungle Bar Lift",
    Default = false,
    Callback = function(Value)
        jungleBarLiftToggle = Value
        if Value then
            task.spawn(function()
                while jungleBarLiftToggle do
                    game.Players.LocalPlayer.Character:SetPrimaryPartCFrame(CFrame.new(-8678.05566, 14.5030098, 2089.25977))
                    game:GetService("ReplicatedStorage").rEvents.machineInteractRemote:InvokeServer("useMachine", workspace.machinesFolder["Jungle Bar Lift"].interactSeat)
                    task.wait(0.1)
                end
            end)
        end
    end
})

local jungleSquatToggle = false
FarmJungleGroup:AddToggle("JungleSquat", {
    Text = "Auto Jungle Squat",
    Default = false,
    Callback = function(Value)
        jungleSquatToggle = Value
        if Value then
            task.spawn(function()
                while jungleSquatToggle do
                    game.Players.LocalPlayer.Character:SetPrimaryPartCFrame(CFrame.new(-8374.25586, 34.5933418, 2932.44995))
                    game:GetService("ReplicatedStorage").rEvents.machineInteractRemote:InvokeServer("useMachine", workspace.machinesFolder["Jungle Squat"].interactSeat)
                    task.wait(0.1)
                end
            end)
        end
    end
})

local EquipGroup = Tabs.Main:AddRightGroupbox("Auto Equip")

local equipWeightToggle = false
EquipGroup:AddToggle("EquipWeight", {
    Text = "Auto Equip Weight",
    Default = false,
    Callback = function(Value)
        equipWeightToggle = Value
        if Value then
            task.spawn(function()
                while equipWeightToggle do
                    local weightTool = game.Players.LocalPlayer.Backpack:FindFirstChild("Weight")
                    if weightTool then
                        weightTool.Parent = game.Players.LocalPlayer.Character
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

local equipPushupsToggle = false
EquipGroup:AddToggle("EquipPushups", {
    Text = "Auto Equip Pushups",
    Default = false,
    Callback = function(Value)
        equipPushupsToggle = Value
        if Value then
            task.spawn(function()
                while equipPushupsToggle do
                    local pushupsTool = game.Players.LocalPlayer.Backpack:FindFirstChild("Pushups")
                    if pushupsTool then
                        pushupsTool.Parent = game.Players.LocalPlayer.Character
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

local equipSitupsToggle = false
EquipGroup:AddToggle("EquipSitups", {
    Text = "Auto Equip Situps",
    Default = false,
    Callback = function(Value)
        equipSitupsToggle = Value
        if Value then
            task.spawn(function()
                while equipSitupsToggle do
                    local situpsTool = game.Players.LocalPlayer.Backpack:FindFirstChild("Situps")
                    if situpsTool then
                        situpsTool.Parent = game.Players.LocalPlayer.Character
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

local equipHandstandToggle = false
EquipGroup:AddToggle("EquipHandstand", {
    Text = "Auto Equip Handstands",
    Default = false,
    Callback = function(Value)
        equipHandstandToggle = Value
        if Value then
            task.spawn(function()
                while equipHandstandToggle do
                    local handstandTool = game.Players.LocalPlayer.Backpack:FindFirstChild("Handstand")
                    if handstandTool then
                        handstandTool.Parent = game.Players.LocalPlayer.Character
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

-- ============ ROCK FARM ============
local RockGroup = Tabs.Main:AddLeftGroupbox("Rock Farm")

function gettool()
	for i, v in pairs(game.Players.LocalPlayer.Backpack:GetChildren()) do
		if v.Name == "Punch" and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid") then
			game.Players.LocalPlayer.Character.Humanoid:EquipTool(v)
		end
	end
	game:GetService("Players").LocalPlayer.muscleEvent:FireServer("punch", "leftHand")
	game:GetService("Players").LocalPlayer.muscleEvent:FireServer("punch", "rightHand")
end

RockGroup:AddToggle("RockJungle10M", {
    Text = "Auto Punch Jungle Rock (10M)",
    Default = false,
    Callback = function(state)
        getgenv().autoFarm = state
        while getgenv().autoFarm do
            task.wait()
            if game:GetService("Players").LocalPlayer.Durability.Value >= 10000000 then
                for i, v in pairs(game:GetService("Workspace").machinesFolder:GetDescendants()) do
                    if v.Name == "neededDurability" and v.Value == 10000000 and game.Players.LocalPlayer.Character:FindFirstChild("LeftHand") and game.Players.LocalPlayer.Character:FindFirstChild("RightHand") then
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 1)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 1)
                        gettool()
                    end
                end
            end
        end
    end
})

RockGroup:AddToggle("RockKing5M", {
    Text = "Auto Punch King Rock (5M)",
    Default = false,
    Callback = function(state)
        getgenv().autoFarm = state
        while getgenv().autoFarm do
            task.wait()
            if game:GetService("Players").LocalPlayer.Durability.Value >= 5000000 then
                for i, v in pairs(game:GetService("Workspace").machinesFolder:GetDescendants()) do
                    if v.Name == "neededDurability" and v.Value == 5000000 and game.Players.LocalPlayer.Character:FindFirstChild("LeftHand") and game.Players.LocalPlayer.Character:FindFirstChild("RightHand") then
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 1)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 1)
                        gettool()
                    end
                end
            end
        end
    end
})

RockGroup:AddToggle("RockLegend1M", {
    Text = "Auto Punch Legend Rock (1M)",
    Default = false,
    Callback = function(state)
        getgenv().autoFarm = state
        while getgenv().autoFarm do
            task.wait()
            if game:GetService("Players").LocalPlayer.Durability.Value >= 1000000 then
                for i, v in pairs(game:GetService("Workspace").machinesFolder:GetDescendants()) do
                    if v.Name == "neededDurability" and v.Value == 1000000 and game.Players.LocalPlayer.Character:FindFirstChild("LeftHand") and game.Players.LocalPlayer.Character:FindFirstChild("RightHand") then
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 1)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 1)
                        gettool()
                    end
                end
            end
        end
    end
})

RockGroup:AddToggle("RockInferno750K", {
    Text = "Auto Punch Inferno Rock (750K)",
    Default = false,
    Callback = function(state)
        getgenv().autoFarm = state
        while getgenv().autoFarm do
            task.wait()
            if game:GetService("Players").LocalPlayer.Durability.Value >= 750000 then
                for i, v in pairs(game:GetService("Workspace").machinesFolder:GetDescendants()) do
                    if v.Name == "neededDurability" and v.Value == 750000 and game.Players.LocalPlayer.Character:FindFirstChild("LeftHand") and game.Players.LocalPlayer.Character:FindFirstChild("RightHand") then
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 1)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 1)
                        gettool()
                    end
                end
            end
        end
    end
})

RockGroup:AddToggle("RockMythical400K", {
    Text = "Auto Punch Mythical Rock (400K)",
    Default = false,
    Callback = function(state)
        getgenv().autoFarm = state
        while getgenv().autoFarm do
            task.wait()
            if game:GetService("Players").LocalPlayer.Durability.Value >= 400000 then
                for i, v in pairs(game:GetService("Workspace").machinesFolder:GetDescendants()) do
                    if v.Name == "neededDurability" and v.Value == 400000 and game.Players.LocalPlayer.Character:FindFirstChild("LeftHand") and game.Players.LocalPlayer.Character:FindFirstChild("RightHand") then
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 1)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 1)
                        gettool()
                    end
                end
            end
        end
    end
})

RockGroup:AddToggle("RockFrost150K", {
    Text = "Auto Punch Frost Rock (150K)",
    Default = false,
    Callback = function(state)
        getgenv().autoFarm = state
        while getgenv().autoFarm do
            task.wait()
            if game:GetService("Players").LocalPlayer.Durability.Value >= 150000 then
                for i, v in pairs(game:GetService("Workspace").machinesFolder:GetDescendants()) do
                    if v.Name == "neededDurability" and v.Value == 150000 and game.Players.LocalPlayer.Character:FindFirstChild("LeftHand") and game.Players.LocalPlayer.Character:FindFirstChild("RightHand") then
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 1)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 1)
                        gettool()
                    end
                end
            end
        end
    end
})

RockGroup:AddToggle("RockTiny0", {
    Text = "Auto Punch Tiny Rock (0)",
    Default = false,
    Callback = function(state)
        getgenv().autoFarm = state
        while getgenv().autoFarm do
            task.wait()
            if game:GetService("Players").LocalPlayer.Durability.Value >= 0 then
                for i, v in pairs(game:GetService("Workspace").machinesFolder:GetDescendants()) do
                    if v.Name == "neededDurability" and v.Value == 0 and game.Players.LocalPlayer.Character:FindFirstChild("LeftHand") and game.Players.LocalPlayer.Character:FindFirstChild("RightHand") then
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.RightHand, 1)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 0)
                        firetouchinterest(v.Parent.Rock, game:GetService("Players").LocalPlayer.Character.LeftHand, 1)
                        gettool()
                    end
                end
            end
        end
    end
})

-- ============ STATS TAB ============
local function abbreviateNumber(value)
    if value >= 1e15 then
        return string.format("%.1fQa", value / 1e15)
    elseif value >= 1e12 then
        return string.format("%.1fT", value / 1e12)
    elseif value >= 1e9 then
        return string.format("%.1fB", value / 1e9)
    elseif value >= 1e6 then
        return string.format("%.1fM", value / 1e6)
    elseif value >= 1e3 then
        return string.format("%.1fK", value / 1e3)
    else
        return tostring(value)
    end
end

local StatsLeftGroup = Tabs.Stats:AddLeftGroupbox("Time & Strength")
local StatsRightGroup = Tabs.Stats:AddRightGroupbox("Durability & Agility")
local StatsLeftGroup2 = Tabs.Stats:AddLeftGroupbox("Kills & Karma")

local labels = {
    TimeSpentLabel = StatsLeftGroup:AddLabel("Time: 00:00"),
    StrengthGainedLabel = StatsLeftGroup:AddLabel("Strength: 0"),
    DurabilityGainedLabel = StatsRightGroup:AddLabel("Durability: 0"),
    AgilityGainedLabel = StatsRightGroup:AddLabel("Agility: 0"),
    KillsGainedLabel = StatsLeftGroup2:AddLabel("Kills: 0"),
    EvilKarmaGainedLabel = StatsLeftGroup2:AddLabel("Evil Karma: 0"),
    GoodKarmaGainedLabel = StatsLeftGroup2:AddLabel("Good Karma: 0")
}

local function createMyLabels()
    local player = game.Players.LocalPlayer
    if not player then return end

    local leaderstats = player:WaitForChild("leaderstats")
    if not leaderstats then return end

    local strengthStat = leaderstats:WaitForChild("Strength")
    local durabilityStat = player:WaitForChild("Durability")
    local agilityStat = player:WaitForChild("Agility")
    local killsStat = leaderstats:WaitForChild("Kills")
    local evilKarmaStat = player:WaitForChild("evilKarma")
    local goodKarmaStat = player:WaitForChild("goodKarma")

    local initialStrength = strengthStat.Value or 0
    local initialDurability = durabilityStat.Value or 0
    local initialAgility = agilityStat.Value or 0
    local initialKills = killsStat.Value or 0
    local initialEvilKarma = evilKarmaStat.Value or 0
    local initialGoodKarma = goodKarmaStat.Value or 0

    local startTime = tick()

    local function updateLabels()
        local strengthGained = strengthStat.Value - initialStrength
        local durabilityGained = durabilityStat.Value - initialDurability
        local agilityGained = agilityStat.Value - initialAgility
        local killsGained = killsStat.Value - initialKills
        local evilKarmaGained = evilKarmaStat.Value - initialEvilKarma
        local goodKarmaGained = goodKarmaStat.Value - initialGoodKarma

        labels.StrengthGainedLabel:SetText("Strength: " .. abbreviateNumber(strengthGained))
        labels.DurabilityGainedLabel:SetText("Durability: " .. abbreviateNumber(durabilityGained))
        labels.AgilityGainedLabel:SetText("Agility: " .. abbreviateNumber(agilityGained))
        labels.KillsGainedLabel:SetText("Kills: " .. abbreviateNumber(killsGained))
        labels.EvilKarmaGainedLabel:SetText("Evil Karma: " .. abbreviateNumber(evilKarmaGained))
        labels.GoodKarmaGainedLabel:SetText("Good Karma: " .. abbreviateNumber(goodKarmaGained))
    end

    local function updateTimeSpent()
        local timeSpent = tick() - startTime
        local minutes = math.floor(timeSpent / 60)
        local seconds = math.floor(timeSpent % 60)
        labels.TimeSpentLabel:SetText(string.format("Time: %02d:%02d", minutes, seconds))
    end

    strengthStat.Changed:Connect(updateLabels)
    durabilityStat.Changed:Connect(updateLabels)
    agilityStat.Changed:Connect(updateLabels)
    killsStat.Changed:Connect(updateLabels)
    evilKarmaStat.Changed:Connect(updateLabels)
    goodKarmaStat.Changed:Connect(updateLabels)

    game:GetService("RunService").Heartbeat:Connect(updateTimeSpent)

    updateLabels()
end

createMyLabels()

-- ============ CLIENT TAB ============
local MovementGroup = Tabs.Client:AddLeftGroupbox("Movement")

local walkSpeedValue = 16
MovementGroup:AddSlider("WalkSpeed", {
    Text = "Walk Speed",
    Default = 16,
    Min = 1,
    Max = 100,
    Rounding = 1,
    Compact = false,
    Callback = function(Value)
        walkSpeedValue = Value
    end
})

local setSpeed = false
MovementGroup:AddToggle("SetSpeed", {
    Text = "Apply Speed",
    Default = false,
    Callback = function(state)
        setSpeed = state
        while setSpeed do
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = walkSpeedValue
            task.wait(0.1)
        end
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 16
    end
})

local jumpPowerValue = 50
MovementGroup:AddSlider("JumpPower", {
    Text = "Jump Power",
    Default = 50,
    Min = 10,
    Max = 200,
    Rounding = 1,
    Compact = false,
    Callback = function(Value)
        jumpPowerValue = Value
    end
})

local applyJumpPower = false
MovementGroup:AddToggle("ApplyJumpPower", {
    Text = "Apply Jump Power",
    Default = false,
    Callback = function(state)
        applyJumpPower = state
        game.Players.LocalPlayer.Character.Humanoid.UseJumpPower = true
        if applyJumpPower then
            game.Players.LocalPlayer.Character.Humanoid.JumpPower = jumpPowerValue
        else
            game.Players.LocalPlayer.Character.Humanoid.JumpPower = 50
        end
    end
})

local sizeValue = 1
MovementGroup:AddSlider("Size", {
    Text = "Body Size",
    Default = 1,
    Min = 1,
    Max = 10,
    Rounding = 0.1,
    Compact = false,
    Callback = function(Value)
        sizeValue = Value
    end
})

local setSize = false
MovementGroup:AddToggle("SetSize", {
    Text = "Apply Size",
    Default = false,
    Callback = function(state)
        setSize = state
        local char = game.Players.LocalPlayer.Character
        if char then
            local humanoid = char:FindFirstChildOfClass("Humanoid")
            if humanoid then
                if state then
                    humanoid.BodyDepthScale.Value = sizeValue
                    humanoid.BodyHeightScale.Value = sizeValue
                    humanoid.BodyWidthScale.Value = sizeValue
                    humanoid.HeadScale.Value = sizeValue
                else
                    humanoid.BodyDepthScale.Value = 1
                    humanoid.BodyHeightScale.Value = 1
                    humanoid.BodyWidthScale.Value = 1
                    humanoid.HeadScale.Value = 1
                end
            end
        end
    end
})

MovementGroup:AddToggle("LockPosition", {
    Text = "Lock Position",
    Default = false,
    Callback = function(Value)
        if Value then
            local currentPos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
            getgenv().posLock = game:GetService("RunService").Heartbeat:Connect(function()
                if game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = currentPos
                end
            end)
        else
            if getgenv().posLock then
                getgenv().posLock:Disconnect()
                getgenv().posLock = nil
            end
        end
    end
})

local GeneralGroup = Tabs.Client:AddRightGroupbox("General")

GeneralGroup:AddButton({
    Text = "Destroy Ad Teleport",
    Callback = function()
        local part = workspace:FindFirstChild("RobloxForwardPortals")
        if part then
            part:Destroy()
        end
    end
})

GeneralGroup:AddButton({
    Text = "Permanent Shift Lock",
    Callback = function()
        loadstring(game:HttpGet('https://pastebin.com/raw/CjNsnSDy'))()
    end
})

GeneralGroup:AddButton({
    Text = "Anti AFK",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/evxncodes/mainroblox/main/anti-afk", true))()
    end
})

local TradeGroup = Tabs.Client:AddLeftGroupbox("Trading & Pets")

TradeGroup:AddToggle("DisableTrade", {
    Text = "Disable Trade",
    Default = false,
    Callback = function(State)
        if State then
            game:GetService("ReplicatedStorage").rEvents.tradingEvent:FireServer("disableTrading")
        else
            game:GetService("ReplicatedStorage").rEvents.tradingEvent:FireServer("enableTrading")
        end
    end
})

TradeGroup:AddToggle("HidePets", {
    Text = "Hide Pets",
    Default = false,
    Callback = function(State)
        if State then
            game:GetService("ReplicatedStorage").rEvents.showPetsEvent:FireServer("hidePets")
        else
            game:GetService("ReplicatedStorage").rEvents.showPetsEvent:FireServer("showPets")
        end
    end
})

local autoRepToggle = false
TradeGroup:AddToggle("AutoLift", {
    Text = "Auto Lift",
    Default = false,
    Callback = function(State)
        autoRepToggle = State
        if State then
            task.spawn(function()
                while autoRepToggle do
                    game:GetService("Players").LocalPlayer:WaitForChild("muscleEvent"):FireServer("rep")
                    task.wait(0.1)
                end
            end)
        end
    end
})

-- ============ KILLER TAB ============
local PunchGroup = Tabs.Killer:AddLeftGroupbox("Punching")

local autoPunchNoAnim = false
PunchGroup:AddToggle("AutoPunch", {
    Text = "Auto Punch",
    Default = false,
    Callback = function(state)
        autoPunchNoAnim = state
        while autoPunchNoAnim do
            local player = game.Players.LocalPlayer
            local playerName = player.Name
            local punchTool = player.Backpack:FindFirstChild("Punch") or game.Workspace:FindFirstChild(playerName):FindFirstChild("Punch")

            if punchTool then
                if punchTool.Parent ~= game.Workspace:FindFirstChild(playerName) then
                    punchTool.Parent = game.Workspace:FindFirstChild(playerName)
                end
                player.muscleEvent:FireServer("punch", "rightHand")
                player.muscleEvent:FireServer("punch", "leftHand")
            end
            wait()
        end
    end
})

PunchGroup:AddButton({
    Text = "Speed Punch",
    Callback = function()
        local player = game.Players.LocalPlayer
        local punch = player.Backpack:FindFirstChild("Punch")
        local character = game.Workspace:FindFirstChild(player.Name)
        local punch1

        if character then
            punch1 = character:FindFirstChild("Punch")
        end

        if punch and punch:FindFirstChild("attackTime") then
            punch.attackTime.Value = 0
        elseif punch1 and punch1:FindFirstChild("attackTime") then
            punch1.attackTime.Value = 0
        end
    end
})

PunchGroup:AddButton({
    Text = "Normal Punch",
    Callback = function()
        local player = game.Players.LocalPlayer
        local punch = player.Backpack:FindFirstChild("Punch")
        local character = game.Workspace:FindFirstChild(player.Name)
        local punch1

        if character then
            punch1 = character:FindFirstChild("Punch")
        end

        if punch and punch:FindFirstChild("attackTime") then
            punch.attackTime.Value = 0.35
        elseif punch1 and punch1:FindFirstChild("attackTime") then
            punch1.attackTime.Value = 0.35
        end
    end
})

local KillGroup = Tabs.Killer:AddRightGroupbox("Killing")

local whitelist = {}

KillGroup:AddInput("WhitelistPlayer", {
    Text = "Whitelist Player",
    Default = "",
    Numeric = false,
    Finished = true,
    Callback = function(Value)
        local targetPlayer = game.Players:FindFirstChild(Value)
        if targetPlayer then
            whitelist[targetPlayer.Name] = true
        end
    end
})

local autoKill = false
KillGroup:AddToggle("AutoKill", {
    Text = "Auto Kill",
    Default = false,
    Callback = function(bool)
        autoKill = bool
        while autoKill do
            local player = game.Players.LocalPlayer
            for _, target in ipairs(game.Players:GetPlayers()) do
                if target ~= player and not whitelist[target.Name] then
                    local targetChar = target.Character
                    local rootPart = targetChar and targetChar:FindFirstChild("HumanoidRootPart")

                    if rootPart then
                        local rightHand = player.Character and player.Character:FindFirstChild("RightHand")
                        local leftHand = player.Character and player.Character:FindFirstChild("LeftHand")

                        if rightHand and leftHand then
                            firetouchinterest(rightHand, rootPart, 1)
                            firetouchinterest(leftHand, rootPart, 1)
                            firetouchinterest(rightHand, rootPart, 0)
                            firetouchinterest(leftHand, rootPart, 0)
                        end
                    end
                end
            end
            wait(0.1)
        end
    end
})

local targetPlayerName = nil

KillGroup:AddInput("PlayerUsername", {
    Text = "Player Username",
    Default = "",
    Numeric = false,
    Finished = true,
    Callback = function(Value)
        targetPlayerName = Value
    end
})

local killTarget = false
KillGroup:AddToggle("AutoKillPlayer", {
    Text = "Auto Kill Player",
    Default = false,
    Callback = function(bool)
        killTarget = bool
        while killTarget do
            local player = game.Players.LocalPlayer
            local target = game.Players:FindFirstChild(targetPlayerName)

            if target and target ~= player then
                local targetChar = target.Character
                local rootPart = targetChar and targetChar:FindFirstChild("HumanoidRootPart")

                if rootPart then
                    local rightHand = player.Character and player.Character:FindFirstChild("RightHand")
                    local leftHand = player.Character and player.Character:FindFirstChild("LeftHand")

                    if rightHand and leftHand then
                        firetouchinterest(rightHand, rootPart, 1)
                        firetouchinterest(leftHand, rootPart, 1)
                        firetouchinterest(rightHand, rootPart, 0)
                        firetouchinterest(leftHand, rootPart, 0)
                    end
                end
            end
            wait(0.1)
        end
    end
})

-- ============ STATS 2 TAB (TRACK STATS) ============
local TrackStatsGroup = Tabs.Stats2:AddLeftGroupbox("Player Tracker")

local targetPlayer = nil

TrackStatsGroup:AddInput("TrackUsername", {
    Text = "Player Username",
    Default = "",
    Numeric = false,
    Finished = true,
    Callback = function(Value)
        local player = game.Players:FindFirstChild(Value)
        if player then
            targetPlayer = player
        else
            targetPlayer = nil
        end
    end
})

local trackLabels = {
    TargetStats = TrackStatsGroup:AddLabel("Target Stats:"),
    StrengthLabel = TrackStatsGroup:AddLabel("Strength: 0"),
    DurabilityLabel = TrackStatsGroup:AddLabel("Durability: 0"),
    AgilityLabel = TrackStatsGroup:AddLabel("Agility: 0"),
    RebirthsLabel = TrackStatsGroup:AddLabel("Rebirths: 0"),
    KillsLabel = TrackStatsGroup:AddLabel("Kills: 0"),
    EvilKarmaLabel = TrackStatsGroup:AddLabel("Evil Karma: 0"),
    GoodKarmaLabel = TrackStatsGroup:AddLabel("Good Karma: 0"),
    EquippedPetsLabel = TrackStatsGroup:AddLabel("Equipped Pets:"),
}

for i = 1, 8 do
    trackLabels["Pet" .. i .. "Label"] = TrackStatsGroup:AddLabel("Pet" .. i .. ": No pet Equipped")
end

local function updateTargetStats()
    if not targetPlayer then return end

    local leaderstats = targetPlayer:FindFirstChild("leaderstats")
    local goodKarma = targetPlayer:FindFirstChild("goodKarma")
    local evilKarma = targetPlayer:FindFirstChild("evilKarma")

    if leaderstats then
        trackLabels.StrengthLabel:SetText("Strength: " .. abbreviateNumber(leaderstats:FindFirstChild("Strength") and leaderstats.Strength.Value or 0))
        trackLabels.DurabilityLabel:SetText("Durability: " .. abbreviateNumber(targetPlayer:FindFirstChild("Durability") and targetPlayer.Durability.Value or 0))
        trackLabels.AgilityLabel:SetText("Agility: " .. abbreviateNumber(targetPlayer:FindFirstChild("Agility") and targetPlayer.Agility.Value or 0))
        trackLabels.RebirthsLabel:SetText("Rebirths: " .. abbreviateNumber(leaderstats:FindFirstChild("Rebirths") and leaderstats.Rebirths.Value or 0))
        trackLabels.KillsLabel:SetText("Kills: " .. abbreviateNumber(leaderstats:FindFirstChild("Kills") and leaderstats.Kills.Value or 0))
    end

    trackLabels.EvilKarmaLabel:SetText("Evil Karma: " .. abbreviateNumber(evilKarma and evilKarma.Value or 0))
    trackLabels.GoodKarmaLabel:SetText("Good Karma: " .. abbreviateNumber(goodKarma and goodKarma.Value or 0))

    for i = 1, 8 do
        local petValue = targetPlayer:FindFirstChild("pet" .. i)
        if petValue and petValue:IsA("IntValue") and petValue.Value ~= "" then
            trackLabels["Pet" .. i .. "Label"]:SetText("Pet" .. i .. ": " .. petValue.Value)
        else
            trackLabels["Pet" .. i .. "Label"]:SetText("Pet" .. i .. ": No pet Equipped")
        end
    end
end

task.spawn(function()
    while task.wait(0.5) do
        if targetPlayer then
            updateTargetStats()
        end
    end
end)

-- ============ SERVER TAB ============
local RejoinGroup = Tabs.Server:AddLeftGroupbox("Rejoin Options")

RejoinGroup:AddButton({
    Text = "Rejoin Server",
    Callback = function()
        local ts = game:GetService("TeleportService")
        local p = game:GetService("Players").LocalPlayer
        ts:Teleport(game.PlaceId, p)
    end
})

RejoinGroup:AddToggle("JoinLowPlayerServer", {
    Text = "Join Low Player Server",
    Default = false,
    Callback = function(bool)
        if bool then
            local module = loadstring(game:HttpGet("https://raw.githubusercontent.com/LeoKholYt/roblox/main/lk_serverhop.lua"))()
            module:Teleport(game.PlaceId, "Lowest")
        end
    end
})

local GraphicsGroup = Tabs.Server:AddRightGroupbox("Graphics Options")

GraphicsGroup:AddButton({
    Text = "Remove Textures",
    Callback = function()
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("Decal") or v:IsA("Texture") then
                v.Transparency = 1
            end
        end
    end
})

GraphicsGroup:AddButton({
    Text = "Reduce Textures",
    Callback = function()
        settings().Rendering.QualityLevel = 1
    end
})

GraphicsGroup:AddButton({
    Text = "Disable Shadows",
    Callback = function()
        local lighting = game:GetService("Lighting")
        lighting.GlobalShadows = false
    end
})

GraphicsGroup:AddButton({
    Text = "Remove Effects",
    Callback = function()
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") then
                v.Enabled = false
            end
        end
        local lighting = game:GetService("Lighting")
        for _, v in pairs(lighting:GetChildren()) do
            if v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("ColorCorrectionEffect") or v:IsA("BloomEffect") or v:IsA("DepthOfFieldEffect") then
                v.Enabled = false
            end
        end
    end
})

GraphicsGroup:AddButton({
    Text = "Simplify Materials",
    Callback = function()
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("BasePart") and not v:IsA("MeshPart") then
                v.Material = Enum.Material.SmoothPlastic
                if v.Parent and (v.Parent:FindFirstChild("Humanoid") or v.Parent.Parent:FindFirstChild("Humanoid")) then
                else
                    v.Reflectance = 0
                end
            end
        end
    end
})

GraphicsGroup:AddButton({
    Text = "Remove Fog",
    Callback = function()
        local lighting = game:GetService("Lighting")
        lighting.FogEnd = 9e9
    end
})

GraphicsGroup:AddButton({
    Text = "Full Optimization",
    Callback = function()
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") then
                v.Enabled = false
            end
        end
        local lighting = game:GetService("Lighting")
        lighting.GlobalShadows = false
        lighting.FogEnd = 9e9
        lighting.Brightness = 0
        settings().Rendering.QualityLevel = 1
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("Decal") or v:IsA("Texture") then
                v.Transparency = 1
            elseif v:IsA("BasePart") and not v:IsA("MeshPart") then
                v.Material = Enum.Material.SmoothPlastic
                if v.Parent and (v.Parent:FindFirstChild("Humanoid") or v.Parent.Parent:FindFirstChild("Humanoid")) then
                else
                    v.Reflectance = 0
                end
            end
        end
        for _, v in pairs(lighting:GetChildren()) do
            if v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("ColorCorrectionEffect") or v:IsA("BloomEffect") or v:IsA("DepthOfFieldEffect") then
                v.Enabled = false
            end
        end
    end
})

local RebirthGroup = Tabs.Server:AddLeftGroupbox("Rebirth")

local autoRebirth = false
RebirthGroup:AddToggle("AutoRebirth", {
    Text = "Auto Rebirth (Normal)",
    Default = false,
    Callback = function(state)
        autoRebirth = state
        while autoRebirth do
            game:GetService("ReplicatedStorage"):WaitForChild("rEvents"):WaitForChild("rebirthRemote"):InvokeServer("rebirthRequest")
            task.wait(0.1)
        end
    end
})

local autoSizeLoop = nil
RebirthGroup:AddToggle("AutoSize2", {
    Text = "Auto Size 2",
    Default = false,
    Callback = function(state)
        if state then
            autoSizeLoop = task.spawn(function()
                while task.wait(0) do
                    game:GetService("ReplicatedStorage").rEvents.changeSpeedSizeRemote:InvokeServer("changeSize", 2)
                end
            end)
        else
            if autoSizeLoop then
                task.cancel(autoSizeLoop)
                autoSizeLoop = nil
            end
        end
    end
})

RebirthGroup:AddToggle("HideAllFrames", {
    Text = "Hide All Frames",
    Default = false,
    Callback = function(state)
        local rSto = game:GetService("ReplicatedStorage")
        for _, obj in pairs(rSto:GetChildren()) do
            if obj.Name:match("Frame$") then
                obj.Visible = not state
            end
        end
    end
})

RebirthGroup:AddButton({
    Text = "Fast Rebirths",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/0o0o0o0o0o0o0o0o0o0o0o0o/0o0o0o0o/refs/heads/main/Kk"))()
    end
})

local autoGrind = false
RebirthGroup:AddToggle("SpeedGrind", {
    Text = "Speed Grind (No Rebirth)",
    Default = false,
    Callback = function(Value)
        autoGrind = Value
        if not Value then return end
        for i = 1, 12 do
            task.spawn(function()
                while autoGrind do
                    game:GetService("Players").LocalPlayer.muscleEvent:FireServer("rep")
                    task.wait(0.083)
                end
            end)
        end
    end
})

-- ============ TELEPORT TAB ============
local LocationsGroup = Tabs.Teleport:AddLeftGroupbox("Locations")

LocationsGroup:AddButton({
    Text = "Tiny Island",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-31.8626194, 6.0588026, 2087.88672)
    end
})

LocationsGroup:AddButton({
    Text = "Starter Island",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(226.252472, 8.1526947, 219.366516)
    end
})

LocationsGroup:AddButton({
    Text = "Beach",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-365.798309, 44.5082932, -501.618591)
    end
})

LocationsGroup:AddButton({
    Text = "Frost Gym",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-2933.47998, 29.6399612, -579.946045)
    end
})

LocationsGroup:AddButton({
    Text = "Mythical Gym",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(2659.50635, 21.6095238, 934.690613)
    end
})

LocationsGroup:AddButton({
    Text = "Eternal Gym",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-7176.19141, 45.394104, -1106.31421)
    end
})

LocationsGroup:AddButton({
    Text = "Legend Gym",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(4446.91699, 1004.46698, -3983.76074)
    end
})

LocationsGroup:AddButton({
    Text = "Muscle King",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-8626, 15, -5730)
    end
})

LocationsGroup:AddButton({
    Text = "Jungle Gym",
    Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-8137, 28, 2820)
    end
})

-- ============ MISC TAB ============
local MiscGroup = Tabs.Misc:AddLeftGroupbox("Local Player")

MiscGroup:AddToggle("AutoEatEgg30Min", {
    Text = "Auto Eat Protein Egg (30m)",
    Default = false,
    Callback = function(State)
        getgenv().autoEatProteinEggActive = State
        task.spawn(function()
            while getgenv().autoEatProteinEggActive and LocalPlayer.Character do
                local egg = LocalPlayer.Backpack:FindFirstChild("Protein Egg") or LocalPlayer.Character:FindFirstChild("Protein Egg")
                if egg then
                    egg.Parent = LocalPlayer.Character
                    ReplicatedStorage.muscleEvent:FireServer("rep")
                end
                task.wait(1800)
            end
        end)
    end
})

MiscGroup:AddToggle("AutoEatEgg1Hour", {
    Text = "Auto Eat Protein Egg (1h)",
    Default = false,
    Callback = function(State)
        getgenv().autoEatProteinEggActive = State
        task.spawn(function()
            while getgenv().autoEatProteinEggActive and LocalPlayer.Character do
                local egg = LocalPlayer.Backpack:FindFirstChild("Protein Egg") or LocalPlayer.Character:FindFirstChild("Protein Egg")
                if egg then
                    egg.Parent = LocalPlayer.Character
                    ReplicatedStorage.muscleEvent:FireServer("rep")
                end
                task.wait(3600)
            end
        end)
    end
})

-- ============ CREDITS TAB ============
local CreditsGroup = Tabs.Credits:AddLeftGroupbox("Credits")

CreditsGroup:AddLabel("Free Version")
CreditsGroup:AddLabel("Discord: rafraelson")

-- ============ UI SETTINGS ============
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetFolder("NeonHub/MuscleLegendsHub")
SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyTheme(ThemeManager.Themes.Dark)

Window:SelectTab(1)

print("✅ Muscle Legends Hub carregado com sucesso!")
